{
    "name": "shut",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "dev": "bun --watch index.ts",
        "build": "bun build index.ts --outdir=dist --target=node --minify",
        "build:node": "node --loader ts-node/esm --experimental-specifier-resolution=node index.ts",
        "start": "node dist/index.js"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@google/genai": "^1.30.0",
        "discord.js": "^14.25.1"
    }
}
